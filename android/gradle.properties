org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Google Maps SDK key for the Events venue / Partner Directory map previews.
# Left blank so the repo never carries a real secret — set your own key here
# locally (this file stays out of version control once you copy your key in,
# see README.md "Google Maps API" section) or pass -PMAPS_API_KEY=... to
# `flutter build`/`flutter run`.
MAPS_API_KEY=
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
